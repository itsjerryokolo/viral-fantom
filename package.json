{
  "name": "viral-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ itsjerryokolo/viral-polygon",
    "create-local": "graph create --node http://localhost:8020/ itsjerryokolo/viral-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ itsjerryokolo/viral-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 itsjerryokolo/viral-polygon"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.4",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
